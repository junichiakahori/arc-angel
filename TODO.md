# 📝 ArcAngel 開発タスクリスト (TODO.md)

## 🚨 AI専用：自己検閲チェックリスト (AI Self-Checklist)
※ Antigravity は作業終了・返答前に以下の項目を必ずセルフチェックすること。

1. **時刻の整合性**:
    - [x] 書いた時刻は **絶対に1秒たりとも未来ではないか？** (メタデータの `local time` を1秒単位で確認し、`date` コマンドで実証したか)
    - [x] **憶測による未来予測入力**（数分後を見越した入力等）を絶対に行っていないか。
2. **パスの完全動的化**:
    - [x] コード内に `../assets/` のような**ハードコードされた相対パス**が残っていないか。
    - [x] すべてのアセット（img, fetch, video, audio）が `BASE_PATH` を介しているか。
3. **リリース整合性**:
    - [x] `latest/vX.X.html` を `index.html` にコピーした際、`grep` で依存パスの残存を確認したか。
4. **選曲リストの保持順序**:
    - [x] 楽曲追加・更新時、リリース日順（昇順：古い順）が維持されているか。
5. **新規楽曲のパス定義**:
    - [x] mp4 追加時、`songs.json` に対応する `assets/charts/曲ID_難易度.txt` のパスが（空でも）定義されているか。

---

## 📜 開発基本ルール (General Policy)
- [x] **変更管理**: 修正時は必ず `latest/v1.0.html` のみを編集し、`index.html` は安定版として保護する。
- [x] **日時の同期**: 修正時は必ず `version_history.json` の対象バージョンとファイル内時刻を**ユーザーの現在時刻**に合わせる（一分でも未来は厳禁）。
- [x] **自動プッシュ**: 修正後は必ず Git のコミット・プッシュを実行する。
- [x] **ファイル命名**: リソースはすべて **スネークケース (`snake_case`)**。
- [x] **楽曲並び順**: 楽曲追加時は必ず `songs.json` および内部バックアップリストを**リリース日順（昇順）**に維持する。
- [x] **譜面パスの事前定義**: 新曲追加時は `songs.json` に将来用の譜面ファイル名（例：`assets/charts/id_hard.txt`）をあらかじめセットする。
- [x] **MP4追加時の自動同期**: 新規に `.mp4` ファイルを追加した際は、即座に `songs.json` に同名の空の譜面エントリを追加し、整合性を保つ。

## 📊 譜面設計ガイドライン & レーン基準
- **EASY(1)**: Space / **NORMAL(3)**: F,Space,J / **HARD(5)**: D,F,Space,J,K
- **EXPERT(7)**: S,D,F,Space,J,K,L / **MASTER(9)**: フル

---

## 🏹 最近完了したこと (Recently Done - Alpha Phase v0.9 Release)
- [x] **v0.9.1 安定版リリース**: アセットパス動的解決、iOSメールバグ修正。
- [x] **v1.0 開発フェーズへの分岐**: `latest/v1.0.html` の作成と環境設定完了。
- [x] **bokuima EASY 譜面作成**: 純粋なJSON形式での譜面完成。
- [x] **リトライ機能 (Retry) の実装**: リザルト画面から即座に再挑戦できる UI/Logic を完了。
- [x] **鑑賞リプレイ機能 (Autoplay) の実装**: システムによる正確なお手本演奏モードを完備。
- [x] **エディターのシーク同期 & 手動入力内容の維持修正**: 編集体験の改善。
- [x] **プレイ開始時の速度リセット**: 2倍速設定後のプレイで詰まる問題を解消。
- [x] **選曲・タイトル画面の音響バグ修正**: プレビューとBGMが重なる、または前の曲が残る問題を解消。
- [x] **収録曲の並べ替え**: 指示通りリリース日（時系列）順にソート。
- [x] **iOS音量制御の改善**: Web Audio API (GainNode) を導入し、iOS デバイスでの BGM/ビデオの音量制御とフェード機能を実現。

## 🛠 v1.0 開発ロードマップ (Next Steps - Priority order)

### 🔴 Priority A: プレイ体験の高度化 (Game Engine)
- [x] **ユーザー操作の記録・再生ロジック**: プレイ中の入力をバッファに保存し、リプレイ時にそれを再現する機能。
- [x] **判定詳細表示**: FAST/SLOW の表示および、ミリ秒単位の判定誤差の可視化。
- [x] **コンボ演出の強化**: 一定コンボ数ごとに画面が微振動したり、エフェクトが豪華になる演出。

### 🟡 Priority B: カスタマイズ & ツール (UX & Tools)
- [x] **キーコンフィグ機能**: 設定画面からプレイキーを任意に変更可能にする。
- [x] **エディターの密集ノーツ対策**: 同時刻に複数のノーツがある場合の選択・編集UIの改善。
- [x] **楽曲プレビュー**: 選曲画面で楽曲を選択した際、サビから再生されるプレビュー機能。

### 🟢 Priority C: コンテンツ拡充 (Chart Creation)
- [x] 『僕らは今のなかで』(`bokuima`): NORMAL, EXPERT, MASTER の作成。
- [x] 『僕らのLIVE 君とのLIFE』(`borara`): 未作成難易度の増強（全難易度完備）。
- [x] **新規楽曲追加**: 『それは僕たちの奇跡』『Snow Halation』を楽曲リストに追加（譜面は順次作成）。

### 🧊 Priority D: 譜面データの完成 (Chart Finalization)
- [ ] **『Snow Halation』**: 全難易度 (5譜面) のレコーディング。
- [ ] **『それは僕たちの奇跡』**: 全難易度 (5譜面) のレコーディング。
- [ ] **『START:DASH!!』**: EASY, EXPERT, MASTER の作成。
- [ ] **『タカラモノズ』**: HARD 以外の難易度作成。
- [ ] **『A song for You! You? You!!』**: HARD 以外の難易度作成。

## 💡 留意点・メモ
- v1.0 では、より「本家」に近いプレミアムなプレイフィールを目指す。
- タイムスタンプはメタデータの現在時刻を死守すること。
