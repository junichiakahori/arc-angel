# 📝 ArcAngel 開発タスクリスト (TODO.md)

## 📜 開発基本ルール (General Policy)
- [ ] **CHANGELOGの自動更新**: バグ修正や機能追加を行った際は、必ずユーザー向けに `CHANGELOG.md` にその内容を追記する。（明示的な指示がなくても自律的に行う）
- [ ] **バージョン管理フロー**: 
    - **開発中の編集禁止**: いかなるバグ対応・機能追加であっても、作業中は **絶対に `index.html` を直接編集しない** こと。（★重要：修正中は必ず `latest/` フォルダ内の最新バージョン `.html` のみを編集の対象とする）
    - すべての作業は `latest/vX.X.html` (現在は v0.9) のみで行い、動作確認完了後のリリース時のみ `index.html` へ反映する。
- [ ] **日時の厳密な更新（★必須・毎回言われずに行うこと）**: 開発版のファイルに少しでも変更を加えた場合、ユーザーから言われなくても、ファイル修正のタイミングで**必ず自律的に全履歴の日時を更新する**こと。
    - `assets/version_history.json`: 対象バージョン（`v0.9`等）の日時を**ユーザーの現在時刻（メタデータ参照）に合わせて**更新する。**（※絶対に未来時刻を記述しないこと。１分でも未来は不可）**
    - `latest/vX.X.html` (例: v0.9.html): 内部定数 `MY_VERSION` が JSON のキーと一致していることを確認する。
    - （※タイトルタグ `<title>` は JavaScript により JSON から動的に生成されるため、直接編集は不要になりました）
    - （変更を行った同一ターン内に、漏れなくこれらは完遂させること）
- [ ] **楽曲の並び順**: リスト (`songs.json`) への楽曲追加は、必ず μ's のリリース日順（歴史順）に行うこと。
- [ ] **譜面アレンジの高度化**: HARD以上の難易度では、単押し・同時押しに加えて **「ロングノーツ（長押し）」** を積極的に取り入れ、リズムの多様性を出すこと。
- [ ] **ファイル命名規則の統一化**: 
    - **ルート・ドキュメント**: `README.md`, `TODO.md`, `CHANGELOG.md` などは標準的な**全大文字 (UPPER CASE)** を維持。
    - **リソース・アセット**: 楽曲データ、画像、動画、JSON、スクリプト等はすべて **スネークケース (`snake_case`)** に統一し、大文字・キャメラケースを排除する。
- [ ] **進捗の同期**: 作業の節目で必ず Git のコミットと GitHub へのプッシュを確認する。
- [ ] **修正後の自動プッシュ**: バグ修正や機能追加、ソースコードの変更を行ったあとは、ユーザーから言われなくても**必ず自身のターミナルツールを用いて `git add . && git commit -m "..." && git push` を実行する** こと。（不要な確認待ちを減らし、シームレスな体験を提供する）

## 📊 譜面設計ガイドライン & レーン使用基準 (Chart Design Guidelines)
- **EASY**:
    - **レーン**: 1レーン (センターのみ：`Space`)
    - **要素**: 単押し・ロングノーツ主体。リズムは表拍主体（4分・2分音符中心）。低密度。
- **NORMAL**:
    - **レーン**: 3レーン (センター3つのみ：`F`, `Space`, `J`)
    - **要素**: 単押し・ロングノーツ。8分音符のリズムを少し混ぜ、リズムに変化をつける。中密度。
- **HARD**:
    - **レーン**: 5レーン (5つ使用：`D`, `F`, `Space`, `J`, `K`)
    - **要素**: **コード（同時押し）の解禁**。8分音符のメロディなぞり。高密度。
- **EXPERT**:
    - **レーン**: 7レーン (7つ使用：`S`, `D`, `F`, `Space`, `J`, `K`, `L`)
    - **要素**: 高密度な乱打、階段、トリル、複雑なロングノーツの組み合わせ。16分音符の導入。
- **MASTER**:
    - **レーン**: 9レーン (フル使用：`A`, `S`, `D`, `F`, `Space`, `J`, `K`, `L`, `;`)
    - **要素**: 人間の限界に挑む超高密度。複雑な認識難パターン、最大級の難易度。

---

## 🏹 最近完了したこと (Recently Done - Alpha Phase v0.9 Release)
- [x] **v0.9 安定版としての正式リリース**: `latest/v0.9.html` を `index.html` へ昇格。
- [x] **モバイルUXの革命的進化**:
    - [x] **iPhone用スリムHUD**: 3段スタック構造とシークバーの極限薄型化により、スマホ縦画面での操作盤干渉を最小化。
    - [x] **iOSメール互換性修正**: `mailto:` 送信時のデータ改行が iOS で消える問題を CRLF エンコードで解決。
- [x] **システム自動化の完遂**:
    - [x] `<title>` タグおよびバージョン表記の完全動的化（JSON連動）。
    - [x] 安定版/開発版の相互誘導ロジック（switcher）の実装。
- [x] **エディターの表示品質向上**:
    - [x] ショートカット一覧の動的表示制御（PCかつエディターモード時のみ、z-index 600 で最前面表示）。
- [x] **その他重大バグ修正**:
    - [x] エディターの描画ループ多重発生（CPU負荷増大）の解消。
    - [x] テストプレイ開始時の即死（MISS判定）バグの解消。
    - [x] ランチャー復帰後の操作不能バグの解消。

## 🛠 現在進行中・次のタスク (Next Steps)
- [ ] **譜面作成 (Chart Creation)**: 
  - [ ] 『僕らは今のなかで』(`bokuima`) の HARD 以外の難易度（EASY, NORMAL, EXPERT, MASTER）の作成。
  - [ ] 『僕らのLIVE 君とのLIFE』(`borara`) の譜面増強。
- [ ] **UIの更なる洗練**:
  - [ ] エディター上でのノーツ配置の視認性向上（重なっているノーツの判別など）。
- [ ] **追加検討機能（低優先度）**:
  - [ ] **キーコンフィグ設定画面**の実装（PC向け：入力キーのカスタマイズ）。
  - [ ] **リプレイ機能**（自分のプレイを後で見返す機能）。

## 💡 留意点・メモ
- **バージョン管理方針**: `v0.9` は安定版となった。次の大きな変更は `v1.0` を検討。
- 開発版の修正は常に `latest/v0.9.html` (または新規 `v1.0.html`) で行い、`index.html` への直接編集は厳禁。
- タイムスタンプは常にユーザーの現在時刻（メタデータ）を参照し、未来時刻を避ける。
